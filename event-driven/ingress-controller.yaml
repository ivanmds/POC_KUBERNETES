apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-controller-general
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /

spec:
  rules:
  - host: kube.acessobank-stg.com.br
    http:
      paths:
      # - path: /mongodb
      #   pathType: Prefix
      #   backend:
      #     service:
      #       name: mongo-service
      #       port:
      #         number: 32767
      - path: /customer
        pathType: Prefix
        backend:
          service:
            name: customer-service
            port:
              number: 30001